stages:
  prepare:
    cmd: python src/prepare.py data/raw/telco_churn.csv data/prepared
    deps:
      - data/raw/telco_churn.csv
      - src/prepare.py
    outs:
      - data/prepared
  train:
    cmd: python src/train.py data/prepared models
    deps:
      - data/prepared
      - src/train.py
    outs:
      - models